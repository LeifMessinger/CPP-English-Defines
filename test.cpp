#include "english_compiled.h"

The function that returns an integer also named abs takes(an integer named x){
	if(x is less than zero) The function will return negative x;
	otherwise The function will return x;
}

start{
	The integer bruh is equal to negative one;

	bruh is equal to abs(bruh);

	Then print "The absolute value of -1 is ";

	Then print bruh then endline;
}
